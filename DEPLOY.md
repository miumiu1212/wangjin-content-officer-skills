# 网金内容官 Skills 部署说明

## 文件结构

```
skills/
├── 网金内容官/                  # 主 skill（完整工作流）
│   ├── SKILL.md
│   └── references/
│       ├── platform-specs.md     # 平台内容规范
│       ├── style-presets.md      # 预置风格定义
│       ├── outline-template.md   # 大纲结构模板
│       ├── compliance-rules.md   # 合规规则清单
│       └── hot-topics-format.md  # 热点数据格式规范
├── 今日热点/                    # 子 skill（仅查热点）
│   └── SKILL.md
├── 选题推荐/                    # 子 skill（热点+选题）
│   └── SKILL.md
├── 生成文章/                    # 子 skill（直接生成文章）
│   └── SKILL.md
└── DEPLOY.md                    # 本文件
```

## 部署方式

### 1. 管理员部署（统一部署）

管理员将 `skills/` 目录下所有 skill 文件夹统一部署到 workbuddy 的 skill 目录中，所有员工自动获得使用权限。

### 2. 员工自行部署

员工可在 workbuddy 的「技能管理」中搜索「网金内容官」及相关子 skill，点击「添加」即可完成部署。

### 3. 部署后验证

部署完成后，员工在 workbuddy 对话中输入以下命令验证：

| 验证命令 | 预期结果 |
|---------|---------|
| `/今日热点` | 返回今日财联社热点卡片 |
| `/选题推荐` | 返回热点选择 → 选题推荐 |
| `/生成文章 测试主题` | 进入平台选择流程 |
| `/网金内容官` | 启动完整工作流 |

## Skill 清单

| Skill 名称 | 命令 | 功能 | 依赖 |
|-----------|------|------|------|
| 网金内容官 | `/网金内容官` 或 `@网金内容官` | 完整工作流：热点→选题→风格→大纲→文章 | 需部署全部 references |
| 今日热点 | `/今日热点` | 仅获取今日财联社热点 | 无 |
| 选题推荐 | `/选题推荐` | 热点+选题推荐 | 无 |
| 生成文章 | `/生成文章` | 直接生成文章（跳过热点/选题） | 无 |

## 外部依赖

| 依赖项 | 说明 | 必须 |
|--------|------|------|
| 财联社数据接口 | 热点数据来源，需授权 | 是 |
| LLM 服务 | 大纲/文章生成 | 是 |
| 合规规则引擎 | 敏感词/风险检测 | 是（可降级） |
| workbuddy 消息卡片能力 | 卡片交互展示 | 是 |

## 配置项

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 热点缓存更新时间 | 08:00 / 12:00 / 16:00 | 定时缓存时间点 |
| 缓存过期阈值 | 2 小时 | 超过此时间触发实时补充 |
| 选题推荐数量 | 3-5 个 | 每次生成的候选选题数 |
| 热点展示数量 | 5-8 条 | 每次展示的热点条数 |
| 草稿保存路径 | `drafts/{员工ID}/articles/` | 草稿文件存储路径 |
